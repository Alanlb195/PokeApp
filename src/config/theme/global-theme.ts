import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
    globalMargin: {
        marginHorizontal: 15
    },
    globalFab: {
        position: 'absolute',
        bottom: 20,
        right: 20
    }
})